import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anynines.ionixt',
  appName: 'ionixt',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
